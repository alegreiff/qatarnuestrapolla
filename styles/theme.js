import { extendTheme } from '@chakra-ui/react';

export const pollaTheme = extendTheme({
  colors: {
    primary: '#ff9671',

    polla: {
      blanco: '#f4f0f5',
      negro: '#323032',
    },
  },
});
